.row
  .col-sm-6.col-md-4.col-md-offset-4
    %table.table.table-striped
      %tr
        %th Nombre
      - @clients.each do |client|
        %tr
          %td= client.name

= link_to 'Nuevo Cliente', new_client_path, {class: 'btn btn-primary', role: 'button'}