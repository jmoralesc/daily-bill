.row
  .col-sm-6.col-md-4.col-md-offset-4
    = form_for @instalment, html: {class: 'form-horizontal', role: 'form', multipart: true} do |form|
      = form.collection_select :client_id, Client.order(:name),:id,:name, {prompt: 'Elige un cliente'}, { id: 'cselection', class: 'form-control' }
      = form.collection_select :bill_id, [],:id,:amount, {prompt: 'Elige una contrata'}, { id: 'bselection', class: 'form-control bill-selection'}
      = form.text_field :amount, placeholder: 'Cantidad', class: 'form-control'
      / = form.text_field :date, class: "form-control filter-input", id: "adate", :autocomplete => :off, placeholder: "Filter"
      = form.submit "Crear" , class: 'btn btn-default'